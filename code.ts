type ChartType = 'area' | 'bar' | 'pie'
type Params = {
  chartType: ChartType
  stackedMode: boolean
  horizontal: boolean
  darkMode: boolean
  showLegend: boolean
  showGrid: boolean
  showChartTitle: boolean
  chartTitle: string
  showXAxisTitle: boolean
  xAxisTitle: string
  showYAxisTitle: boolean
  yAxisTitle: string
  chartWidth: number
  chartHeight: number
  seriesCount: number
  pointCount: number
}

// placeholder — actual file is in Figma custom-tools folder
// See the compiled code.js for the live version
export {}
